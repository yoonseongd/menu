import React, { Component } from "react";
import FinalTemplate from "./FinalTemplate";

export default New;

const New = ({ counter, todos }) => {
  return <FinalTemplate nav={"new"} />;
};

export default New;
